apiVersion: v1
kind: Service
metadata:
  name: pgadmin
  namespace: linksy
spec:
  selector:
    app: pgadmin
    deployment: pgadmin
  ports:
    - protocol: TCP
      port: 5050
      targetPort: 80
  type: ClusterIP